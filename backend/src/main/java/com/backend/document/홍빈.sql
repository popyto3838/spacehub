#홍빈

DROP TABLE AUTH;

ALTER TABLE MEMBER
    ADD COLUMN AUTH_NAME ENUM ('ADMIN', 'HOST', 'USER');

ALTER TABLE MEMBER
    ADD COLUMN WITHDRAWN CHAR(1) DEFAULT 'N';

SELECT *
FROM MEMBER;

SELECT*
FROM AUTH;